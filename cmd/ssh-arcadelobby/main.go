// Command ssh-arcadelobby is the SSH front door for the ssharcade fleet:
// one public address, an arcade menu, and a transparent bridge into the
// selected game's own SSH server.
//
// The implementation is planned in docs/ — see docs/README.md for the
// architecture, the identity-forwarding protocol, and per-task specs. This
// stub exists so the module builds from day one.
package main

import "fmt"

func main() {
	fmt.Println("ssh-arcadelobby: not yet implemented — see docs/README.md")
}
