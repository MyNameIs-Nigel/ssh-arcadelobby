# Per-game release workflow template (docs/04-deployment-and-cicd.md).
# Copy to <game-repo>/.github/workflows/release.yml and replace every
# <GAME> / <game> placeholder: image name, compose service name, and the
# go-version-file path if it differs. ci.yml is identical across every
# repo (vet + build + go test -race ./...) — copy this repo's verbatim.
#
# Same shape as this repo's own release.yml: test (never skipped) →
# publish (needs: test) → deploy (needs: publish, runs on the host's own
# self-hosted runner — see "Self-hosted runner setup" in deploy/README.md
# in ssh-arcadelobby. Every game repo registers its own runner instance on
# the host; there's no account-wide runner pool on GitHub's free tier for
# personal accounts).
name: Release

on:
  push:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
      - run: go vet ./...
      - run: go build ./...
      - run: go test -race ./...

  publish:
    needs: test
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v4
      - uses: docker/setup-buildx-action@v3
      - uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - id: meta
        run: echo "sha_short=sha-$(git rev-parse --short HEAD)" >> "$GITHUB_OUTPUT"
      - uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          tags: |
            ghcr.io/mynameis-nigel/<repo>:latest
            ghcr.io/mynameis-nigel/<repo>:${{ steps.meta.outputs.sha_short }}

  deploy:
    needs: publish
    runs-on: [self-hosted, production]
    environment: production
    steps:
      - name: Deploy <service>
        run: |
          cd /srv/ssharcade
          docker compose pull <service>
          docker compose up -d <service>
